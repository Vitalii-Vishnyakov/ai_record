# Offline Audio Transcriber (WhisperKit + NaturalLanguage)

Приложение для **офлайн-транскрибации аудиозаписей** на iOS 17.  
Проект написан на **Swift** с использованием **UIKit**, **AVFoundation**, **WhisperKit** и **NaturalLanguage**.

Основная идея: пользователь записывает голос как на диктофоне, приложение офлайн расшифровывает аудио в текст и при необходимости делает краткое резюме (summary) на устройстве.

---

## Основные возможности

- 🎙 Запись аудио через `AVAudioRecorder`
- ▶️ Воспроизведение последней аудиозаписи через `AVAudioPlayer`
- 🧠 Офлайн-транскрибация с помощью **WhisperKit**
- ✂️ Локальная текстовая суммаризация через **NaturalLanguage**
- 🧾 Отображение:
  - пути к сохранённому аудиофайлу
  - расшифрованного текста
  - краткого резюме текста
- 📱 Интерфейс на **UIKit**:
  - отдельный `RecordViewController` с кнопками:
    - Начать запись
    - Закончить запись
    - Запустить проигрывание
    - Остановить проигрывание
    - Запустить тестовую расшифровку / суммаризацию
  - длинные пути к файлам выводятся в `UILabel` внутри `UIScrollView`

---

## Стек технологий

- **Язык:** Swift
- **Платформа:** iOS 17+
- **UI:** UIKit, Auto Layout
- **Аудио:**
  - `AVFoundation` (`AVAudioSession`, `AVAudioRecorder`, `AVAudioPlayer`)
- **Транскрибация:**
  - [WhisperKit] — офлайн-модель распознавания речи (Whisper)
- **Обработка текста:**
  - `NaturalLanguage` (NLTagger, NLEmbedding, возможные собственные простые summarizer-алгоритмы)
- **Архитектура (примерно):**
  - `RecordViewController` — UI и пользовательские действия
  - `AudioRecorderService` — запись аудио
  - `AudioPlayerService` — воспроизведение аудио
  - `TranscriptionService` — обёртка над WhisperKit
  - `SummarizationService` — обёртка над NaturalLanguage

---

## Требования

- **Xcode:** 16+ (под iOS 17 SDK)
- **iOS:** 17.0 и выше
- **Устройство:** iPhone с поддержкой iOS 17
- **Интернет:** нужен только для первого скачивания модели (если WhisperKit загружает её из сети).  
  Распознавание и суммаризация выполняются **офлайн**.

---

## Установка и запуск

1. Склонировать репозиторий:

   ```bash
   git clone https://github.com/username/offline-transcriber.git
   cd offline-transcriber

